name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Build the project
        run: npm run build

      - name: Deploy to GitHub Pages
        run: |
          # Xóa remote origin nếu nó đã tồn tại
          git remote rm origin || echo "Remote 'origin' does not exist, skipping..."
          
          # Cấu hình thông tin người dùng cho git
          git config --global user.name "GitHub Actions"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Thêm lại remote origin
          git remote add origin https://github.com/${{ github.repository }}.git
          
          # Thực hiện các lệnh git
          git add --all
          git commit -m "Deploy to GitHub Pages"
          git push https://github.com/${{ github.repository }}.git HEAD:gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
